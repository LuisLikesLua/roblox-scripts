# roblox-scripts
 a list of roblox scripts I have made or found (will specify) for executors like xeno
## TO USE
 click on any of the files, if it looks like `loadstring(game:HttpGet("big-link"))()` then just copy and run that.
 if it looks like regular code, then click the `raw` button, copy the link it redirects you to, and put it inside this template<br>
 `loadstring(game:HttpGet("[REDIRECT LINK]"))()`<br>
 and then run it
## Made by me? list
 ESP.lua
